import { DataSource, Repository } from 'typeorm';
import { Booking } from './entities/booking.entity';
import { CreateBookingDto } from './dto/create-booking.dto';
import { UpdateBookingDto } from './dto/update-booking.dto';
import { CheckinDto } from './dto/checkin.dto';
import { CheckoutDto } from './dto/checkout.dto';
import { FindBookingsDto } from './dto/find-bookings.dto';
import { Property } from '../properties/entities/property.entity';
import { CostCenter } from '../cost-centers/entities/cost-center.entity';
import { Unit } from '../units/entities/unit.entity';
import { Tenant } from '../tenants/entities/tenant.entity';
import { CheckinCheckoutLog } from './entities/checkin-checkout-log.entity';
export declare class BookingsService {
    private readonly repo;
    private readonly propertyRepo;
    private readonly costCenterRepo;
    private readonly unitRepo;
    private readonly tenantRepo;
    private readonly logRepo;
    private readonly dataSource;
    constructor(repo: Repository<Booking>, propertyRepo: Repository<Property>, costCenterRepo: Repository<CostCenter>, unitRepo: Repository<Unit>, tenantRepo: Repository<Tenant>, logRepo: Repository<CheckinCheckoutLog>, dataSource: DataSource);
    create(dto: CreateBookingDto): Promise<Booking>;
    private mapBookingRegisterRow;
    findAll(query: FindBookingsDto): Promise<{
        items: {
            id: string;
            propertyId: string;
            propertyName: string;
            propertyCode: string;
            costCenterId: string;
            costCenterCode: string;
            costCenterName: string;
            unitId: string;
            unitNo: string;
            unitTypeCode: string;
            unitTypeName: string;
            unitStatus: string;
            tenantId: string;
            tenantName: string;
            tenantPhone: string;
            tenantEmail: string;
            tenantCpr: string;
            bookingType: string;
            bookingSource: string;
            status: string;
            checkInDate: string;
            checkOutDate: string;
            contractStartDate: string;
            contractEndDate: string;
            rateDaily: number;
            rateMonthly: number;
            depositAmount: number;
            discountAmount: number;
            notes: string;
            createdAt: string;
            updatedAt: string;
        }[];
        total: number;
        page: number;
        limit: number;
    }>;
    findOne(id: string): Promise<{
        id: string;
        propertyId: string;
        propertyName: string;
        propertyCode: string;
        costCenterId: string;
        costCenterCode: string;
        costCenterName: string;
        unitId: string;
        unitNo: string;
        unitTypeCode: string;
        unitTypeName: string;
        unitStatus: string;
        tenantId: string;
        tenantName: string;
        tenantPhone: string;
        tenantEmail: string;
        tenantCpr: string;
        bookingType: string;
        bookingSource: string;
        status: string;
        checkInDate: string;
        checkOutDate: string;
        contractStartDate: string;
        contractEndDate: string;
        rateDaily: number;
        rateMonthly: number;
        depositAmount: number;
        discountAmount: number;
        notes: string;
        createdAt: string;
        updatedAt: string;
    }>;
    private requireBookingEntity;
    update(id: string, dto: UpdateBookingDto): Promise<Booking>;
    remove(id: string): Promise<{
        deleted: boolean;
    }>;
    checkIn(id: string, dto: CheckinDto): Promise<{
        message: string;
        booking: Booking;
    }>;
    checkOut(id: string, dto: CheckoutDto): Promise<{
        message: string;
        booking: Booking;
        extraCharges: number;
    }>;
}
