# Same-origin API proxy: leave NEXT_PUBLIC_API_BASE_URL unset so the UI calls /api/v1 → Nest (port 3000).
# Fixes login when you open the app as http://127.0.0.1:3001 or a LAN IP (browser "localhost" was wrong host).
NEXT_PUBLIC_API_ORIGIN=http://127.0.0.1:3000
NEXT_PUBLIC_SHOW_DEV_LOGIN_HINT=1
